#ifndef PLOTFILE_H_INCLUDED
#define PLOTFILE_H_INCLUDED
#include "discpp.h"
#include <iostream>
using namespace std;


//void plot(double* x,double *y,int n);
void plot(double* y,int n);
double mini(const double *arr, int length);
double maxi(const double *arr, int length);

#endif // PLOTFILE_H_INCLUDED
